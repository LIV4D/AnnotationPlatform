import { BackgroundCanvas } from './background-canvas.model';

describe('BackgroundCanvas', () => {
  it('should create an instance', () => {
    expect(new BackgroundCanvas()).toBeTruthy();
  });
});
